{
  "name": "malayalam-translation-workbench",
  "version": "0.1.0",
  "private": true,
  "description": "Editorial workspace for Malayalam Bible-note translation, review, and print export.",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "check": "node --check app.js && node --check api/db.js && node --check api/save.js && node --check api/projects.js && node --check api/translate.js && node --check api/health.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google-cloud/translate": "^9.2.0",
    "@neondatabase/serverless": "^1.0.2",
    "word-extractor": "^1.0.4"
  },
  "devDependencies": {
    "vercel": "^39.2.5"
  }
}
